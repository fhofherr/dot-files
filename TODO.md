* [ ] Module asdf fails when install is called twice.
* [ ] Modules need a way to clear their state when install is called.
* [ ] Regenerate full environment config even if a single module is
  executed.
